---
paths:
  - "<module-glob — e.g. modules/feature/practice/**>"
---

# <module-name> rules

<!-- Generated by /fx-init as a starting point; edit freely — /fx-init never
     overwrites an existing rules file. Loads only when a file matching `paths:`
     is READ (not on write/create — see docs-meta/runbook.md for the caveat and
     the live test). Keep under ~40 lines: invariants, not documentation. -->

- Entry point: `<path to open first>`
- <module invariant — DI scope, threading rule, feature-flag gate — edit me>
- <forbidden pattern — edit me>
